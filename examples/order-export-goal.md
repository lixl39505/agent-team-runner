# 订单导出

为管理后台增加订单 CSV 导出能力。

必须包含:

- 后端按时间范围和订单状态导出
- 无导出权限时返回明确错误
- 前端可以发起导出并显示失败信息
- 单元测试覆盖空结果、超限和权限错误

明确不实现:

- 异步任务队列
- Excel 格式
- 生产环境部署

限制:

- 不修改数据库结构
- 不升级依赖
- `package.json` 和锁文件由 Integrator 之外的角色禁止修改
